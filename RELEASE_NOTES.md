# SrvDesk v1.0.114

## 中文

- 检查更新窗口加大，状态长文可换行，避免按钮/底栏盖字
- 常用软件安装进度条文案加高，不再被进度条裁切
- 启动时侧栏按完整标签文案留宽（含图标/加粗），避免只露半截字
- 单文件 `SrvDesk.exe`，轻度混淆；请以管理员身份运行

## English

- Larger Check-for-updates dialog; status text wraps so footer/buttons do not cover it
- Common software install progress label height fixed (no clipping by the bar)
- On startup, sidebar width fits full nav labels (icons + bold), not truncated text
- Single-file `SrvDesk.exe`, lightly obfuscated; run as Administrator
