# SrvDesk v1.0.22

## 中文

- 垃圾清理改为分组勾选（缓存 / 系统残留 / 临时文件），带进度与文件数统计
- 对照 ZyperWin++ 清理项；WinSxS 不做 ResetBase；较重项默认不勾
- 帮助 → 检查更新：从 GitHub Releases 下载 `SrvDesk.exe`，替换后自动重启
- 启动时可检测新版本（文件 → 程序设置 可关闭）

下载：单文件 `SrvDesk.exe`，请以管理员身份运行。

## English

- Junk cleanup is now a grouped checklist (cache / system leftovers / temp) with progress
- Cleanup items aligned with ZyperWin++; no WinSxS ResetBase; heavy items off by default
- Help → Check for updates downloads `SrvDesk.exe` from GitHub Releases and restarts
- Optional startup update check (File → Settings)

Download `SrvDesk.exe` and run as Administrator.
