---
description: 每次编译都要把 exe 拷到仓库 dist
alwaysApply: true
---

# 编译输出到 dist

`dotnet build` / `publish.ps1` 之后，exe 必须出现在仓库根目录 `dist\`。

- 工程已有 `CopyExeToDist` 目标，会调用 `scripts/copy-to-dist.ps1`
- 优先覆盖 `dist\SrvDesk.exe`；若文件被正在运行的进程锁住，则写成 `dist\SrvDesk-HHmmss.exe`
- 不要只把结果留在 `src\SrvDesk\bin\` 或 `obj\`
- `dist/` 已 gitignore，不要提交进去
