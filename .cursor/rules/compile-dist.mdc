---
description: 编译输出到 dist，交付 exe 必须经 Obfuscar 轻度混淆
alwaysApply: true
---

# 编译输出到 dist + Obfuscar

## 交付用 exe（默认）

凡要给用户用的 `dist\SrvDesk.exe`，**必须**走轻度混淆，不要只用 `dotnet build`：

```powershell
.\scripts\publish.ps1 -NoBumpVersion
```

- 需要升补丁版本时：去掉 `-NoBumpVersion`（脚本会改 csproj 版本）
- 脚本内部：`dotnet publish` → **Obfuscar**（`scripts/obfuscar.xml`）→ 写入 `dist\`
- **禁止**默认加 `-SkipObfuscate`（仅调试排查混淆问题时才可临时使用，且勿当交付包）

## dist 约定

- 优先覆盖 `dist\SrvDesk.exe`；若被锁住则写成 `dist\SrvDesk-HHmmss.exe`
- 不要只把结果留在 `src\SrvDesk\bin\` 或 `obj\`
- `dist/` 已 gitignore，不要提交进去

## 本地快速验证（非交付）

仅自己改代码看效果时可用 `dotnet build -c Release`（CopyExeToDist 会拷到 dist），但**交付前务必再跑一遍 `publish.ps1`**。
